# 视觉配对自适应预算：离线首轮筛选

暂停语义路线。不下载新数据，不训练模型，不重新提取图像特征。
固定候选顺序与已有Pair-VPR分数，从top20结果决定是否扩展到top44。
决策函数只接收20个已计算分数，禁止输入后24个分数和GT。

首轮仅测试top1与top2配对分数差（不称为校准概率）。在GSV2048上不用GT计算
差值的25%、50%、75%分位阈值，低差值查询扩展到44。50%是主要规则，
另外两条为探索性预算点；全部阈值先落盘锁定，再读Pitts评分，不能事后挑赢家。
Pitts直接使用GSV绝对阈值，报告真实扩展率，不能在Pitts重算分位点。
同地点的多个好候选也可能差值很低，这正是该简单假设可能失败的原因。

对照：固定K=20到44的完整精度—配对数曲线，以及固定query-ID哈希随机扩展。
随机扩展概率对应GSV设定比例，不保证Pitts实际成本匹配，因此同时提供自适应平均
成本两侧的固定K对照。报告R1、纠错/退化、平均配对数、双向前向次数、相对44节省率。
Pitts按全景分组bootstrap（10000次seed42）比较自适应与44及向上取整成本的固定K。
不使用“不显著”冒充等效；不预先宣称达到非劣效或稳定收益。

第一轮只衡量准确率与配对次数，不能推断墙钟加速，因为动态调度改变dense缓存命中率。
GSV/Pitts都已历史暴露，属于探索性迁移；本轮没有独立验证或新颖性证明。
若主要规则被同等/更低成本固定K支配，就不部署、不继续在Pitts调阈值。

训练机CPU执行（本机不测试）：

```bash
python -m pytest -q tests/test_adaptive_pair_budget.py
python -u scripts/adaptive_pair_budget.py fit
python -u scripts/adaptive_pair_budget.py eval
```

输出doc/adaptive_pair_budget_v1。旧缓存代码不修改；所有源完成清单和评分分片须校验。
只追踪代码和文档，原始评分缓存不进Git。

## 首轮结果（2026-09-22）

实现提交113c81d；训练机5项单元测试通过，CPU离线完成，报告下载后完成清单SHA256校验通过。
GSV锁定阈值分别0.3126554489、0.8291349411、2.2589535713；Pitts未重新拟合。

| 数据 | 方法 | 平均配对数 | 正确查询 |
| --- | --- | ---: | ---: |
| GSV /2048 | 固定20 | 20 | 1994 |
| GSV /2048 | 主规则margin50% | 32 | 1995 |
| GSV /2048 | 固定32 | 32 | 2001 |
| GSV /2048 | 固定44 | 44 | 2003 |
| Pitts /7608 | 固定20 | 20 | 7338 |
| Pitts /7608 | 主规则margin50% | 38.5394 | 7348 |
| Pitts /7608 | 固定38 | 38 | 7349 |
| Pitts /7608 | 固定44 | 44 | 7353 |

主要规则在GSV被同成本固定32支配，在Pitts被更低成本固定38支配，故本轮不部署。
Pitts主要规则比44少5条正确，相对44少12.41%配对；分组差值95%区间
[-0.17189,+0.03931] pp，跨零不能当成等效证明。
探索性75%规则在Pitts得到7353正确、平均42.6309配对，但不能事后取代预定主要规则：
只省约3.11%配对，而且在GSV平均38配对仅2001正确，与固定38相同，也不胜过固定32的2001。
探索性25%规则GSV1995/平均26，Pitts7342/平均30.877；不据此继续在Pitts扫阈值。

结论仅限简单top1-top2差值停止假设：它既可能反映同地点多正样本竞争，
也可能漏掉“前20很自信但第21—44存在更好匹配”的查询。上述机制是解释假设，尚未因果验证。
跨域绝对分数尺度变化导致扩展率明显漂移。当前没有证据证明该规则优于固定预算取舍，
不宣称自适应路线全部无效，不宣称语义收益，不新增训练或数据下载。

## 前20高分差错误诊断（2026-09-22）

脚本scripts/diagnose_pair_budget.py（提交ff69f5e），训练机8项相关测试通过，CPU执行；
报告doc/pair_budget_diagnostic_v1下载后SHA256核验通过。没有改阈值、选新策略、训练或用GPU。

| 现象 | GSV | Pitts |
| --- | ---: | ---: |
| top44纠错总数 | 11 | 21 |
| margin主要规则漏掉的纠错 | 8 | 10 |
| 扩展总数 | 1024 | 5877 |
| 扩展中前两名均为GT正样本 | 998 | 5618 |
| 实际执行的退化 / top44全部退化 | 2/2 | 1/6 |
| 漏纠错查询最高分中位数 | -2.2980 | 5.5063 |
| 稳定正确查询最高分中位数 | 19.7073 | 21.2725 |

低分差常对应多个GT有效正参考，不等于需要扩大搜索。“高置信错误”在这里仅指
高相对分差，而不是高校准正确概率；落选纠错查询可能只有一个相对较好的弱候选。
这是评分层诊断，不是图像重叠、动态遮挡或模型因果机制的视觉证明。

在top20错误查询内部，较低最高分与top44可纠正性有关，按负最高分计算的描述性AUC：
GSV约0.6892，Pitts约0.7657；原始分差的较大值AUC仅0.5835/0.5198。
这些统计条件包含不可部署的“已知top20错误”，因此不能当作真实调度器性能；
GSV仅11个纠错正例、Pitts仅21个，并有历史暴露和查询相关性，无显著性或稳定性声明。
赢家全局排名信号在GSV/Pitts不一致（0.5603/0.7398），不据Pitts挑特征。

后续可预先锁定一个低最高分触发扩展的单变量假设，在GSV确定规则后迁移，
继续用等成本固定K与随机对照；不把本次统计直接转换成已验证的策略，
不在Pitts事后扫阈值，也不把绝对分数当校准概率。本轮仅完成诊断，未启动新策略实验。
